package com.example.studentchecker

import android.os.Bundle
import androidx.fragment.app.Fragment
import android.view.LayoutInflater
import android.view.View
import android.view.ViewGroup

/**
 * Этот экран инициализируется всегда, если заходит Преподаватель не во время сканирования.
 * По кнопке @+id/btnMenu из toolbar_top.xml адо пересылать пользователя на настройки.
 * А теперь кнопочка, которые я запихаю в TODO :3
 * Больше ничего делать тут не надо, разве что картинку менять.
 */
class ScanningInactiveFragment : Fragment() {
    // TODO: подменяй картинку в темной теме на scan_dark.png
    /** TODO:
     *  по кнопке @+id/btnStart запускаешь сканирование и отправляешь юзера на [ScanningFragment]
     *  Если вырублен Bluetooth, уведомление кидаешь внизу стандартное:
     *  "Невозможно запустить сканирование. Включите Bluetooth"
     *  Просто скопируй и вставь.
     */
}